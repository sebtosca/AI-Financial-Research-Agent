# Trivy vulnerability exceptions.
# Add CVE IDs here only with a comment explaining why they're accepted
# (e.g. no fix available, not exploitable in this deployment).

# perl-base (Debian OS package in the python:3.12-slim base image).
# No fixed version available upstream as of 2026-07-18. Re-check when the
# base image is rebuilt/updated.
CVE-2026-13221
CVE-2026-42496
CVE-2026-8376

# chromadb 1.3.4 -- arbitrary code execution via pre-authentication code
# injection. No fixed version listed in the advisory as of 2026-07-18.
# Not remotely exposed in this deployment (used only as an in-process
# vector store client, no ChromaDB server port is ever opened). Re-check
# for a patched release before upgrading chromadb.
CVE-2026-45829
