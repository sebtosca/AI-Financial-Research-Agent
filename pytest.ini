[pytest]
markers =
    integration: marks tests that call live APIs (deselect with -m "not integration")
    live: marks opt-in tests that require credentials and external services
    slow: marks tests that may take significant time or incur higher API cost
